# === published site (GitHub Pages root) ===
# build/ IS the deployed site — keep it tracked.
# Internal links are relative to build/, so it must be committed as-is.

# === authoring inputs / heavy derived artifacts (not part of the site) ===
# raw audio sources (build/tts-audios/ is the deployed copy, generated by portal-render)
tts-audios/
# but NEVER ignore the deployed audio inside build/ — it's part of the site
!build/tts-audios/

# PDF exports + packaged zip
pdf/
*.zip

# notebookLM workspace noise
notebookLM/

# archmage scratch notes
_archmage-notes/

# === dead/orphan build artifacts (replaced by portal-nav.js) ===
build/nav-data.js
build/nav-controller.js

# === general noise ===
*.tmp
*.swp
*~
