# === published site (GitHub Pages root) ===
# docs/ IS the deployed site root (/docs is a GitHub Pages preset) — keep tracked.
# Internal links are relative to docs/, so it must be committed as-is.

# === authoring inputs / heavy derived artifacts (not part of the site) ===
# raw audio sources (docs/tts-audios/ is the deployed copy, generated by portal-render)
tts-audios/
# but NEVER ignore the deployed audio inside docs/ — it's part of the site
!docs/tts-audios/

# PDF exports + packaged zip
pdf/
*.zip

# notebookLM workspace noise
notebookLM/

# archmage scratch notes
_archmage-notes/

# === dead/orphan build artifacts (replaced by portal-nav.js) ===
docs/nav-data.js
docs/nav-controller.js

# === general noise ===
*.tmp
*.swp
*~
